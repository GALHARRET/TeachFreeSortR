% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap_mds.R
\name{bootstrap_mds}
\alias{bootstrap_mds}
\title{Bootstrap MDS configurations with Procrustes alignment}
\usage{
bootstrap_mds(data, k = 2, n_boot = 200, method = "classical", sc = FALSE)
}
\arguments{
\item{data}{A matrix or data.frame with products in rows and individuals
in columns (group labels), as used in \code{\link{total_dissim}}.}

\item{k}{Number of MDS dimensions. Defaults to \code{2}.}

\item{n_boot}{Number of bootstrap replicates. Defaults to \code{200}.}

\item{method}{MDS method, passed to \code{\link{compute_mds}}.
Defaults to \code{"classical"}.}

\item{sc}{Scaling or not the configuration.}
}
\value{
A data.frame with columns \code{product}, \code{Dim1}, ...,
\code{DimK}, \code{replicate}, suitable for plotting with
\code{ggplot2::stat_ellipse()} grouped by \code{product}.
}
\description{
Performs bootstrap resampling over individuals to assess the stability of
an MDS configuration. Each bootstrap replicate's configuration is aligned
to the reference configuration via Procrustes analysis before being
collected, allowing confidence ellipses to be drawn per product.
}
